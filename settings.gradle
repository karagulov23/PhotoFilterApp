pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()

    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()

        //for ImagePicker
        maven { url "https://jitpack.io" }

        //for PhotoEditor
        flatDir {
            dirs 'libs'
        }
    }
}
rootProject.name = "PhotoFilterApp"
include ':app'
